import React from 'react';
import FlagpoleAttendancePage from '../../components/FlagpoleAttendance/FlagpoleAttendancePage';

/**
 * Wrapper page component for Flagpole Attendance
 * This allows for future additions like layout, breadcrumbs, etc.
 */
const FlagpoleAttendance = () => {
    return (
        <div>
            <FlagpoleAttendancePage />
        </div>
    );
};

export default FlagpoleAttendance;