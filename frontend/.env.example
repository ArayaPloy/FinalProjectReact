# Environment Variables Template
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามต้องการ

# ===========================================
# Development Environment
# ===========================================

# API Base URL (Backend URL)
# Development: http://localhost:5000/api
# Production: https://your-backend-domain.com/api
VITE_API_BASE_URL=http://localhost:5000/api

# ===========================================
# หมายเหตุ
# ===========================================
# 1. ไฟล์ .env จะไม่ถูก commit ขึ้น Git (อยู่ใน .gitignore)
# 2. ใน Vite ตัวแปร env ต้องขึ้นต้นด้วย VITE_ เท่านั้น
# 3. หลังแก้ไขไฟล์นี้ ต้อง restart dev server
# 4. สำหรับ Production ให้ตั้งค่าใน hosting platform dashboard

# ===========================================
# ตัวอย่าง Production URLs
# ===========================================
# Vercel:    VITE_API_BASE_URL=https://your-api.vercel.app/api
# Heroku:    VITE_API_BASE_URL=https://your-app.herokuapp.com/api
# Railway:   VITE_API_BASE_URL=https://your-app.railway.app/api
# Custom:    VITE_API_BASE_URL=https://api.yourdomain.com/api
