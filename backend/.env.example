# ============================================
# Backend Environment Variables Template
# ============================================
# คัดลอกไฟล์นี้เป็น .env และแก้ไขค่าตามต้องการ

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
# MongoDB Connection String
# Development: ใช้ local MongoDB
# Production: ใช้ MongoDB Atlas

# Local MongoDB (Development)
MONGODB_URL=mongodb://localhost:27017/eduweb

# MongoDB Atlas (Production) - ตัวอย่าง
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/eduweb?retryWrites=true&w=majority

# ============================================
# Authentication
# ============================================
# JWT Secret Key - ใช้สำหรับ sign JWT tokens
# Production: ใช้ strong random string (อย่างน้อย 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Expiration
JWT_EXPIRES_IN=7d

# ============================================
# CORS Configuration
# ============================================
# Frontend URL สำหรับ CORS
# Development
FRONTEND_URL=http://localhost:5173

# Production - เพิ่ม URLs อื่นๆ ถ้าจำเป็น
# FRONTEND_URL=https://your-frontend-domain.com

# ============================================
# File Upload Configuration
# ============================================
# Maximum file size (in bytes)
MAX_FILE_SIZE=5242880

# Allowed file types
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/gif,image/webp

# ============================================
# Email Configuration (ถ้ามีระบบส่งอีเมล)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ============================================
# หมายเหตุ
# ============================================
# 1. ไฟล์ .env จะไม่ถูก commit ขึ้น Git (อยู่ใน .gitignore)
# 2. อย่าแชร์ค่า JWT_SECRET กับผู้อื่น
# 3. Production ต้องใช้ strong passwords และ secrets
# 4. MongoDB Atlas: ต้องสร้าง cluster และ database user ก่อน
